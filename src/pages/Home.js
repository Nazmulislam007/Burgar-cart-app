import { Stack } from "@mui/material";
import React from "react";
import { Navbar } from "../components";

const Home = () => {
  return (
    <Stack spacing={2}>
      <Navbar />
    </Stack>
  );
};

export default Home;
